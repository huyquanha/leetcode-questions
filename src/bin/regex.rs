// https://leetcode.com/problems/regular-expression-matching/description/

fn main() {

}

fn is_match(s: String, p: String) -> bool {
  let mut s_iter = s.chars();
  let mut p_iter = p.chars();
  let previous_p: Option<char> = None;
  loop {
    let next_p = p_iter.next();

  }
}